# カラーシステム設計仕様書 v1.3

> 2026-08-17 策定。v1.0 →（レビュー1回目）→ v1.1 →（同2回目）→ v1.2 →（明るめ階段の追加）→ v1.3。すべて同日。
>
> **2026-08-18 面の改名**: `input → item`。入力欄だけの面ではなく「面の上に載るもの」全般
> （入れ子の容れ物・タグなど）に使うため。Akuarium も同じ概念を item と呼んでいた。
>
> **2026-08-18 アクセント改名（一部）**: `lime → turtle` / `cyan → jelly` / `coral は旧 purple`。
> 残る6色は仮名のまま。値は変えていない。
>
> **2026-08-18 テーマ改名**: `grey → hadal` / `slate → trench` / `nord-ish → fjord` / `dim → shoal`。
> 水域の名前にした（深いほど光が届かず面の色が薄くなる順）。値は一切変えていない。
> テーマ名 `dim` と階段名 `dim` の衝突もこれで解消した。
>
> **v1.3 の変更**: ★**L 階段が2系統になった**（`dark` = bg L15 / `dim` = bg L26）。しろさまの「純粋なライトテーマは作らないが、ライトモードユーザーには明るめの青グレーを投げる」という判断による。詳細は §2 P1 と §6。dim は破綻 0件・アクセント9色すべて使用可。
> 表示: プレビューサイト `pages/`（提出書 `tools/palette/index.html` は 2026-08-18 に撤去）。値は制約から解いたもの）
> 一次記録: `reference/color-system-oklch.md` / 経緯: `log/2026-08-17.md` / wixdex 実測: `reference/wixdex-ui-audit.md`
>
> ★数値はすべて実測・実計算値。再現手順は §14。

---

## §0 v1.2 の変更点（レビュー2回目への対応）

### 決まったこと（しろさまの判断）

| 論点 | 決定 |
|---|---|
| **直交性（任意テーマ × 任意アクセント）は要件か手段か** | ★**要件**。「ディストロとかその後アプリとかいろいろに使うこと考えると、ユーザーの選択権は大事」（本人）。**ただし「しろさまが設計時にレバーを弄る自由」は要件ではない**ので切り離した（下記の運用変更） |
| **nord のトーン天井** | ★**nord-ish で行く**（B案）。「nord はあくまで雰囲気の例だから ish でいい」（本人）。※2026-08-18 に `fjord` へ改名（Nord 由来を残したまま水域名にした）。Nord 本家の C ≤ 0.121 を超えることを**明示的に許容**する |
| **ΔE フロアの値** | ★**0.055**。提出書の較正セクションで実物を見て「**ΔE 0.058 でも区別できる**」と判定（本人）。v1.1 の目分量 0.08 から引き下げ |
| **L 階段は入力か出力か** | ★**「共通の目標階段からの最小逸脱」を全トークンに適用**。v1.1 は text-main だけ可変という中途半端な位置にいた |

### 運用の変更

**しろさまがツールを操作するのをやめた。** あたしが制約を解いてパレットを提出し、しろさまは実物を見て「紫もうちょい明るく」のような指示を出す。計算はあたしが引き受ける。

ツールは**ランタイムの制約系ではなく、設計時の検証器 + 提出書**に降格した。**直交性はランタイム（エンドユーザーの選択）に残る**ので、制約ソルバも可用性マトリクスもそのまま必要。

### 修正した指摘

| # | 指摘 | 対応 |
|---|---|---|
| 1 | hover と選択が同一トークンに融合した | **`hover-surface`（ΔE ≥ 0.03）と `selected-surface`（ΔE ≥ 0.055）に分離**。既存の二分探索を2回呼ぶだけ。両者の ΔC は最小 0.023 で JND 以上を確保 |
| 2 | accent-surface のフロアが `input` に対してしか測られていない | **載りうる全面（panel / item / overlay）に対して測る**。★v1.1 の実装は「同じ L の仮想的な面」と比較しており、実在の面と比べていなかった＝**バグでもあった**。実在の面と比べると L 差が稼げるので、jelly の「解なし」も消えた |
| 3 | nord の magenta が Nord 本家の C 上限を超える | **nord-ish と明記して美学の側を緩める**（上記の決定。現 `fjord`） |
| 4 | §11 と §5 が乖離している | `error-surface` 上の文字・枠のペアを §11 に追加。トークン数を §5 と §13 で一致させた |
| 5 | フロアが未較正 | **実物較正を実施し 0.055 に確定**（上記）。ただし CVD の盲点は未対応（§13-C） |

### 未対応（次の工事）

| # | 指摘 | 状態 |
|---|---|---|
| (c) | APCA を advisory で並走させ、WCAG 2 と判定が割れたペアを実機確認リストとして出す | **未着手**。`line-weak`(1.51:1) と `error`(4.59:1) が真っ先に載るはず |
| (d) | ΔE は「意味の混同」の代理として筋が悪い。用途で使い分ける | **未着手**。面の識別は ΔE で妥当だが、意味の混同は色相カテゴリ則 + CVD シミュレーションが要る（§13-C にリスクを明記） |
| (f) | 真実の源泉が3つ（ツール JS / Python / 仕様書の表）ある | **未着手**。★次の最優先工事。正準実装1つ → JSON → CSS・§7/§8/§11 の表・将来のデスクトップ設定を全部生成する。**§12 のプロトコル手順「表を更新する」が手作業である限り、プロトコル自身がドリフトを製造し続ける** |
| (e) | ダーク専用は Mado にとって製品判断 | **保留**。§13-D に記載 |
| (g) | コンポーネント契約を安定化対象にすれば、トークン命名は急がなくてよい | **受け入れ**。§10 のクラス層を契約とし、命名体系の検討は優先度を下げた |

---

## §1 適用範囲

**対象**: wixdex / Mado / YTaq / TABaq / 自作 Linux ディストロのデスクトップシェル。

**スコープ外**: タイポグラフィ / スペーシング / 角丸・影の値 / モーション / アイコンセット / **ドメイン色**（WIXOSS のカード色のように対象物側で固定され作者が動かせない色。wixdex 移行時に第4分類として追加する。出発点は「H は対象物固有で固定 / L はアクセント同様に固定して視認性を検証 / C は上限比で導出 / テーマ倍率は意味色と同じ `+offset` 方式」）。

---

## §2 前提

| # | 前提 | 理由 |
|---|---|---|
| P1 | **純粋なライトテーマは作らない**。代わりに**明るめの L 階段（dim 系統）を用意する** | ★2026-08-17 のしろさまの判断:「**本当に真っ白背景が嫌い。ユーザーが離れるとしても純粋なライトテーマは作りません**」「**ライトモードユーザーには nord 系の明るめの青グレーを投げる**」。→ 極性は反転しない（明るい文字を暗めの面に載せたまま）が、bg を L26 まで持ち上げた第2の階段を持つ。**bg を L30 より上げると本文 4.5:1 を満たす L が色域外に出る**ので、L26 が実用上の上限帯（§6） |
| P2 | **sRGB を基準に彩度上限を決める** | P3 なら余裕は増えるが、非対応環境で破綻しない側に倒す |
| P3 | **コントラスト基準は WCAG 2.2 AA** | APCA は 2023-07 に WCAG 3 ドラフトから外され、勧告は2028〜2030年頃。★**WCAG 2 は暗背景で過大評価する**既知の欠陥があり、本仕様は直撃領域にある。数値のパスは実機確認の代替にならない |
| P4 | **色だけで情報を伝えない**（WCAG 1.4.1） | 意味色はアイコン・文言と併用 |
| P5 | **テーマ × アクセントの直交性は要件** | ディストロのシェルとアプリ群でエンドユーザーが選ぶため。設計時の探索自由度は要件ではない |

---

## §3 設計の骨格

| | 絶対に守る | 目標（守れないときは最小限だけ譲る） |
|---|---|---|
| v1.0 | L 階段の値 | （なし） |
| v1.1 | 色域・コントラスト・ΔE | L 階段（text-main のみ可変） |
| **v1.2** | 色域・コントラスト・ΔE | **L 階段（全トークンが同じ扱い）** |

「L 階段は全テーマ共通で固定」は**維持できない約束**だった。同じ L94 でも、面C と H によって色域に入ったり入らなかったりする。守れない約束を持つと破っても気づけない。

★v1.1 は text-main だけを導出に降格しており、「なぜ text-main だけ?」が説明できなかった。**v1.2 では全トークンを「共通の目標階段からの最小逸脱」として扱う**（実際に逸脱するのは今のところ text-main だけだが、扱いを揃えた）。Leonardo 型（制約グラフのみで階段を持たない）には行かない。テーマ間で L が揃う利点を捨てないため。

### 人が決めるもの

```
[1] L 階段（目標値・全テーマ共通）
[2] テーマ … H / 面C / アクセント強さ
[3] アクセント … H だけ
[4] 意味色 … H と L
```

### 導出（人が値を持たない）

```
各トークンの L    = 目標値。色域に入らなければ入る最大まで下げる
accent C          = maxC(0.74, H) × アクセント強さ
accent-active     = L0.71 で C = maxC(0.71, H) × アクセント強さ（再クランプ）
hover-surface     = 中立。面の色相のまま、全面に対し ΔE ≥ 0.03 を満たす最大 L（下向き探索）
selected-surface  = L33（dark）/ L44（dim）で ΔE ≥ 0.055 を満たす最小 C
selected-neutral-surface  = 同 L で面の彩度のまま（アクセントに依存しない中立版）
semantic C        = maxC(L, H) × min(0.95, アクセント強さ + 0.15)
semantic-surface  = selected-surface と同じ導出
focus-ring        = accent / scrim = L8 のアルファ 0.72（on-accent は出力しない。§5）
```

### 制約

```
1. 全トークンが sRGB 色域内（base / active）
2. §11 の全ペアがコントラスト基準を満たす（状態込み）
3. ΔE(accent 各状態, text-sub)  ≥ 0.08
4. ΔE(accent, 各意味色)         ≥ 0.055   ← 実物較正済み
5. ΔE(hover-surface, 全面)      ≥ 0.03
6. ΔE(selected-surface, 全面)   ≥ 0.055
7. |C(selected) − C(hover)|     ≥ 0.02（2段が互いに区別できる）
```

**満たさないアクセントはそのテーマで選択不可**。提出書が理由を表示する。

---

## §4 なぜ OKLCH か

HSL では「明るさを揃えたまま色相だけ変える」ができない（`hsl(60,100%,50%)` の黄と `hsl(240,100%,50%)` の青は同じ明度指定でも見え方が全く違う）。テーマ切替は「L を揃えて H を振る」が前提なので、HSL では土台から成立しない。

**OKLCH**（2020-12、Björn Ottosson）は L が知覚的にほぼ均等。CIELAB(1976) の青領域の破綻を修正している。Tailwind v4 はデフォルトパレット全体を OKLCH に移行済みで、`oklch()` は Baseline 到達。Tailwind の `zinc` は **H を 285.8〜286.3 に固定し L だけ動かす**設計で、本仕様の面の作り方と同じ。

色相角は HSL とズレる: 赤≈27 / オレンジ≈55 / 黄≈95 / 緑≈148 / シアン≈205 / 青≈255 / 紫≈305 / ピンク≈355。

---

## §5 トークン一覧

★**一覧は `CONTRACT.md`（生成物）を参照**。`solve.py` の `THEME_TOKENS` / `ACCENT_TOKENS` が正で、
この仕様書には表を持たない（§15 と同じ理屈。個数を散文に書くと必ずズレる）。

★2026-08-20 の棚卸しで **`on-accent` / `warning-active` / `success-active` を出力から外した**。
塗るボタンが無いので accent の上に載る文字が無く（チェックのレの字はブラウザが
`accent-color` から自動で決める）、押せる意味色は danger（error）だけのため。
計算は `tokens()` に残してあり、使う部品ができたら `THEME_TOKENS` に足すだけで出る。

★v1.1 の `accent-surface` を **`hover-surface` / `selected-surface` の2段に分割**した。v1.1 では1トークンで「選択行・hover 行・タブの下地」を兼ねており、リスト上で「選択中の行」と「カーソルが乗っている行」が同一に見えた。

★下地トークンの命名規則（2026-08-20 整理）: **下地は `-surface` で終わる**。
中立版は変種なので印を持つ（`selected-neutral-surface`）。旧名 `selected-neutral` から改名した。

---

## §6 L 階段（目標値）— ★2系統

**v1.3 で階段が2系統になった。系統内では共通、系統をまたぐと別。**

★**数値の表は `tools/spec-tables.md`（生成物）を参照**。この仕様書には表を持たない（§15）。

**dim は「ライト志向のユーザーに投げる明るめの階段」**（P1）。極性は反転しない。

- **bg の上限は L30**。L35 以上にすると、overlay 上で本文 4.5:1 を満たす L が 96 以上になり色域から出る
- **dim では accent が L74 → L80 に上がる**。overlay が L45 と明るいため、L74 のままだと overlay 上で 3.13:1 とギリギリになる
- **dim では意味色も上がる**（error 73→85 / warning 82→85 / success 76→83）。代償として **dim の error は `#f4bfbc`（C0.061）と薄い赤**になる。overlay(L45) の上で 4.5:1 を出すには L85 が必要で、L85 では赤に彩度が乗らないため。★実運用で「エラーらしくない」と感じたら、dim では意味色を文言でなく surface + 枠で表現し、文言は `text-main` にする手がある（未実施）

- **`item`(28) は `overlay`(34) より暗い** … 「高度の軸」と「操作可能性の軸」を一本に並べると、モーダル内の入力欄がモーダルより明るくなる。順序で解決した
- **面同士のコントラストは 1.14〜1.47:1** … 意図的。面の分離は border が担保する（WCAG 1.4.11 の対象は「識別に必要な部分」）。§10 のコンポーネント層でデフォルト化する
- **線と文字は「最も明るい面 = overlay(34)」から逆算** … v1.2 では面の L が状態で動かないので、この逆算は hover 中も崩れない
- **`text-disabled`(62) は disabled / placeholder 専用** … overlay 上で 3.23:1。WCAG 1.4.3 の除外規定に依る。v1.0 の `text-faint` から改名したのは、本文に使った瞬間コードレビューで浮かせるため

---

## §7 テーマ

★**テーマの値と使えるアクセントの一覧は `tools/spec-tables.md`（生成物）を参照**。

**面C が上がるとアクセント強さを下げる理由**: Nord を OKLCH に変換した実測が根拠。Nord は全色が C ≤ 0.121 に収まりトーンが統一されており、そこに Tailwind の `orange-500`（C 0.213）を置くと1色だけ別世界に見える。`zinc` は C≈0.01 でトーン制約が存在しないので鮮烈なアクセントが成立する。

★**`fjord`（旧 nord-ish）は Nord 本家の C 上限を超える**（magenta が 0.150）。これは**明示的な選択**で、しろさまの「nord はあくまで雰囲気の例だから ish でいい」という判断による。厳密に Nord のトーンに収めたい場合はテーマに C 天井 0.121 を制約として足すことになるが、その場合 clown と seal が落ちる。

**トレードオフ**: アクセント強さを下げるほど `ΔE(accent, text-sub)` が縮み、使えるアクセントが減る。`fjord` を ×0.72 まで上げれば9色すべて使えるが、Nord らしさは薄れる。

---

## §8 アクセント

```
orange 55 / turtle 122 / teal 188 / jelly 212 / blue 255
indigo 282 / coral 308 / magenta 332 / seal 355
```

- **L は 74 固定**、**C = maxC(0.74, H) × アクセント強さ**
- **信号機カラー（赤・黄・緑）を持たない**。そこは意味色の領分で、衝突したらアクセント側が譲る（意味色は動かせないため）

**なぜ L=74 か（実測）**: 全色相を1つの L で賄うには「最も弱い色相の上限が最大になる L」が最適。

| L | 最弱色相の上限 | 平均 |
|---|---|---|
| 70 | 0.120 (jelly) | 0.183 |
| **74** | **0.126 (jelly)** | 0.170 |
| 78 | 0.114 (blue) | 0.155 |

**なぜ C を絶対値で固定しないか**: L74 での上限は magenta 0.250 / orange 0.182 / jelly 0.129 と倍近く違う。C を 0.15 で固定すると jelly は色域外、magenta は上限の60%になる。上限比で持つことで、どの色相も同じ度合いで振り切る。副次的に色域外が発生しない。

---

## §9 意味色

| | H | L | 根拠 |
|---|---|---|---|
| `error` | 22 | **73** | L70 では overlay 上 4.06:1 で本文基準を割る。L73 で 4.59:1 |
| `warning` | 90 | **82** | 黄は L82 以上でないと黄色に見えない（L60 だとオリーブ `#a1790c`） |
| `success` | 155 | 76 | |

**C = maxC(L, H) × min(0.95, アクセント強さ + 0.15)**。完全固定にすると `fjord` のようにトーンを抑えたテーマで意味色だけが浮きすぎる。`+0.15` により**アクセントより常に強い**ままテーマのトーンに従う。

**意味色は状態を持たない**（hover / active しない）。destructive なボタンは v1.2 のスコープ外。

---

## §10 実装

### 状態の規約

```
hover    : 面の L は動かさない。hover-surface を敷く
selected : selected-surface（または selected-neutral-surface）。
           ★左端2pxのアクセント線は「形」で決まる（2026-08-20 実物判定）――
           既定の行は下地のみ、ruled（連なる一覧）は下地 + 線
active   : --suisou-accent-active（C は再クランプ済み）
focus    : outline: 2px solid var(--suisou-focus-ring); outline-offset: 2px
disabled : --suisou-text-disabled。本文には使わない
```

**hover を「面を明るくする」でやらない理由**（v1.0 の実測）: (a) アクセントが色域外になる（`hadal` で6色）、(b) hover した overlay(L39) の上で line-weak が 1.24:1、line-strong が 2.53:1 に落ちる、(c) accent の hover(L79) が text-sub(L78) と**コントラスト 1.00:1** になる。「C を濃くする」案も teal で ΔC 0.019 と JND 未満のため不採用。

**focus の輪が accent と重ならない理由**: `focus-ring = accent` なので、accent で装飾された
要素にフォーカスが当たると同色が隣接して消えうる。実装は `outline-offset: 2px` ――
輪と要素の間に下地が 2px 見えるので、距離で分離できている（v1.2 で書いていた
「bg の 1px を挟む二重 box-shadow」はこの形に置き換えた）。

### コンポーネント層 ＝ 安定化する契約

**利用側が触るのは `data-suisou-*` 属性であってトークンではない。** §6 の「border 必須」と
§5 の「text-disabled は disabled 専用」を、規約ではなくデフォルトにする（`pages/ui/*.css`）。

★v1.2 でここに書いていたクラス案（`.panel` / `.field` …）は **data 属性の部品に置き換えた**。
凍結対象は **`CONTRACT.md`（生成物）にある属性・値・トークン・既定の目録**で、
破壊的変更はその目録で判定する。

### CSS の形

切替は `data-suisou-theme` と `data-suisou-accent` の2属性（**必ず同じ要素に書く**。
`lint_css.py` が両方向の単独指定を検査する）。アクセントの C・accent-active・下地・
text-main の L・意味色はすべて**計算済みの確定値**を書き出す（CSS では `maxC` を
計算できないため）。色域外の組み合わせだけ出力されず、推奨しない組は★コメント付きで出る（掟2 の運用）。

---

## §11 コントラスト検証

**全テーマ × 使用可能アクセント × 状態（base / active）で、以下の全ペアが基準を満たす。構造的破綻 0件。**

★**検証ペアの一覧と最悪値は `tools/spec-tables.md`（生成物）を参照**。ペアの定義は `solve.py` の `PAIRS` が正。

**要件外**: `text-disabled` on `overlay` = 3.23:1（WCAG 1.4.3 の除外規定）

★この表は WCAG 2 の式による値で、P3 のとおり暗背景では過大評価される。`line-weak`(1.51:1) と `error`(4.59:1) は基準ギリギリで実機確認が必要。**(c) の APCA 並走を実装すれば、この2つが自動で確認リストに載る。**

---

## §12 改訂プロトコル

「L 階段は1回決めたら動かさない」は v1.1 で撤回済み。

1. あたしが `tools/solve.py` の定義を直す
2. `python3 solve.py` を走らせる（`pages/palette.css` / `pages/suisou.data.js` / `spec-tables.md` が再生成される）
3. しろさまが提出書（`index.html`）を開いて実物を見る
4. 「紫もうちょい明るく」のような指示 → あたしが数値に翻訳して 1 に戻る

★**v1.3 で手順から「表を手で更新する」が消えた**。数値の表は仕様書に持たず、すべて `spec-tables.md`（生成物）にある。この仕様書が持つのは**散文と判断の理由だけ**。

★これ以前は手順に手作業の表更新が含まれており、**プロトコル自身がドリフトを製造していた**（v1.0 の色域チェック漏れ、v1.1 の「16トークン」誤記、§11 の検証ペア欠落は、すべてこれが原因）。

---

## §13 既知の制約・未解決

### A. 根拠の質

- **`SEM_OFFSET = 0.15`** … 目分量。「意味色はアクセントより一段強く見せる」という意図で置いた値で、逆算ではない
- **ΔE フロア** … `semantic` は **2026-08-17 に実物較正済み（0.058 が区別可能 → 0.055）**。`hover`(0.03) / `selected`(0.055) / `textSub`(0.08) は未較正の目分量

### B. 数値上の弱点

- **`accent`(L74) と `text-sub`(L78) が近い**。ΔE フロアは満たすが、グレースケールにするとほぼ同じ明るさ。アクセントは枠線・下線・配置と併用する前提
- **アクセント9色に近いペアがある**（magenta–seal 23度 / coral–magenta 24度 / teal–jelly 24度）
- **`turtle` が鮮やかにならない**。L74 では黄系の色域が狭い。L 固定を選ぶ限り構造的に解決しない
- **`line-weak`(1.51:1) と `error`(4.59:1) が基準ギリギリ**

### C. 構造上の弱点

- **ΔE は「意味の混同」の代理としては筋が悪い**。守りたいのは知覚距離ではなく「危険と誤読しないこと」で、これはカテゴリ知覚の問題。ΔE フロアを通っていても、色相カテゴリとしては同族のペアが残る（一覧は `spec-tables.md`）。特に **`clown` は `error`(33度) と `warning`(35度) の両方と同族**。
  - ★**2026-08-17、しろさまが実物で判定 → 許容**。「**dim の error もちゃんとそれぞれの意味色で認識できる**」「**wixdex のオレンジもちゃんと見分けつく。UIUX で充分にカバー可能**」。=**色だけに頼らずアイコン・文言・配置で補う**（P4）という前提のもとで、この距離を受け入れる。カテゴリ則（「意味色の色相帯 ±N度 は同 L 帯のアクセント禁止」）は**導入しない**
  - ただし判定は**しろさま個人の見え方**に基づく。下の CVD は別問題として残る
- ~~★**`selected-surface` に載る罫が検査されていない**~~ → **2026-08-20 解決**。
  `hover-surface` を中立化する検討の副産物として 2026-08-18 に見つかったもの。
  `PAIRS` に `line-weak` / `line-strong` on 選択下地の行が無く、solver が見ていなかった。

  ```
  直す前  hadal / 選択下地(L36)  line-weak 1.37:1（必要 1.5） / line-strong 2.81:1（必要 3.0）
  直した後 最悪               line-weak 1.53:1 / line-strong 3.14:1     （32組すべて）
  ```

  原因は色相ではなく**階段上の位置**だった。`tint`(L36) が `overlay`(L34) より
  **上**にあり、`overlay` は `PAIRS` に入っていて通っていたのに `tint` は
  誰も見ていなかった。2ポイントの差の間に制約の境界があった。

  **直し方**: `tint` を L36 → **L33**（dim は L47 → L44）。
  罫（`line-weak` L44 / `line-strong` L61）を上げる案は行き止まり ――
  `line-strong` を 63 に上げると `text-disabled`(62) を追い越し、
  `line-weak` を 46.5 にすると全パネルの罫が明るくなる。
  `tint` を下げる方が影響が選択下地と意味色の下地に閉じる。

  **代償**: 使える組み合わせが 33/36 → **32/36**。`trench × jelly` が L35 の時点で
  選択下地の解（ΔE ≥ 0.055）を失う。走査した結果**両立する L は存在しない**ので、
  掟2（コントラスト基準は絶対）を優先して組み合わせを1つ捨てた。
  既定の `hadal × jelly` は無事。

  以前ここに「`trench` は帯が存在しない → `tint` の L を動かすだけでは解決しない」と
  書いていたが、**あれは「9色すべてを残す」前提での話**だった。1色捨てる前提なら帯はある。
  前提を書かずに結論だけ残していたのが誤り。

  ★あわせて `PAIRS` に7行足した（選択下地の罫4本 ＋ 中立版の文字1本）。
  今後 `tint` や罫の段を動かせば `--check` が落ちる。

- ~~★**保留**: 選択下地をアクセント色のままにするか中立にするか~~ → **2026-08-20 解決**。
  A/B をプレビューに出してしろさまが判定 → **「どっちのパターンも使いたい」**。
  数値では決められないことが実測で確定していた（明度が同じなので hover との区別 ΔE 0.18/0.17、
  コントラスト 2.81/2.87 とほぼ同値。違うのは彩度だけ）。
  よって**両方出して現場が選ぶ**形にした。2026-08-18 の「部品ごとに違いそう」が正解だった。

    --suisou-selected-surface   アクセント色（既定）
    --suisou-selected-neutral-surface   中立。明るさだけで示す

  中立版はアクセントに依存しないので `THEME_TOKENS` 側。`hover` との JND 検査も足した ――
  中立版は色相が同じで差が明度だけなので、L の段を詰めたときに真っ先に壊れる。

- ★**CVD（色覚特性）の盲点**（未対応）。Oklab の ΔE は色覚特性をモデル化していない。しろさま本人が見分けられても、**2型色覚のユーザーには当てはまらない**（赤と緑が近づくため、`turtle`/`teal` と `success`、`clown`/`seal` と `error` が危険側）。Viénot / Brettel のシミュレーションを通した後の ΔE でも同じフロア検証を回す必要がある。**上の実物判定はこの穴を埋めない**
- **`text-disabled` の用途制限は、命名とコンポーネント層でしか守れない**。lint による機械的検査は未整備
- **面の階層は border 必須という規約に依存**。コンポーネント層で緩和したが、生のトークンを直接使えば破れる
- **Helmholtz–Kohlrausch 効果を考慮していない**。高彩度の色は同じ L でも明るく見える（特に黄）。Oklab は輝度ベースでこの効果を扱わない
- **WCAG 2 の暗背景での不正確さ**（P3）

### D. 未着手

- ★**単一生成源(f)** … 次の最優先。正準実装1つ → JSON → CSS・仕様書の表・デスクトップ設定を全部生成する
- **APCA の advisory 並走(c)**
- **カテゴリ則 + CVD シミュレーション(d)**
- **wixdex / Mado への適用**。wixdex は現状 gray と zinc が二重化し色クラスが69種類（本仕様は20トークン）
- **ドメイン色**（§1）
- **タイポグラフィとスペーシングのスケール**。色の L 階段は等差でよい（L は知覚均等）が、**サイズは等比（モジュラースケール）が理屈に合う**（知覚が対数的なため）
- ~~**Mado をダーク専用のままにするか(e)**~~ → ★**決着（2026-08-17）**。「**本当に真っ白背景が嫌い。ユーザーが離れるとしても純粋なライトテーマは作りません**」「**ライトモードユーザーには nord 系の明るめの青グレーを投げる**」。=**Mado は dim 系統の最初の顧客**になる。純粋なライトは作らず、離脱は引き受ける
- **destructive なボタン**（意味色に状態を持たせる必要が出る）
- **トークンの命名体系**（§10 でクラス契約を凍結対象にしたため優先度は下がった）

---

## §15 生成の仕組み（真実の源泉は1つ）

```
tools/
  solve.py             ← ★正準実装。色を決めるのはここだけ
  spec-tables.md       ← 生成物。本仕様書の数値表と検証結果はすべてここにある
  lint_css.py          ← 手書き側に色が混入していないか検査する
pages/
  palette.css          ← 生成物。CSS 変数（色 + 色コードの文字列）
  suisou.data.js       ← 生成物。テーマ/アクセント/可用性の構造。色は持たない
  index.html / suisou.css / app.js  ← 手書き。var(--suisou-*) しか参照しない
```

★**2026-08-18、提出書（`palette.data.js` + `index.html`）は撤去した。** 表示はプレビューサイトが
担い、検証結果は `spec-tables.md` が持つため、役割が重複していた。

- **`solve.py` を直して `python3 solve.py` を走らせる**。それ以外の場所で色を定義しない
- `python3 solve.py --check` は生成せず検証だけ行い、破綻があれば exit 1（CI に入れられる）
- **この仕様書は散文と判断の理由を持ち、数値の表は持たない**

★v1.2 までは「ツール(JS) / Python 実装 / 仕様書の表」の3つが同じ数値を主張し、同期は手作業だった。**今日出たミス3件はすべてこの構造が原因**（色域チェック漏れ / トークン数の誤記 / 検証ペアの欠落）。

---

## §14 再現方法

旧提出書に全実装（JS）があったが撤去済み。**現行の正準実装は `tools/solve.py`**（このリポジトリ内で完結。
OKLCH 変換の一次記録は ccmemory 側 `reference/color-system-oklch.md` にあり、ここには無い）。
検算済み（白 → L=100%/C=0、`#808080` → L=60%/C=0、黒 → L=0%）。

```
lin(L,C,H)     OKLCH → linear sRGB
inG(L,C,H)     色域内か
maxC(L,H)      その L と H で sRGB に収まる最大 C（二分探索）
cr(a,b)        WCAG 2 の相対輝度比
dE(a,b)        Oklab のユークリッド距離。JND ≒ 0.02
tint(...)      載りうる全面に対し ΔE フロアを満たす最小 C（二分探索）
```

**検証項目**: 全トークンが色域内（base / active）/ §11 の全ペアが全テーマ × 使用可能アクセント × 全状態で基準を満たす / §3 の制約7つを全組み合わせで満たす / 新テーマ追加時に L が自動で下がって色域に収まる。
